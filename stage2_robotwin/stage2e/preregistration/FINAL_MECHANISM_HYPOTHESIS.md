# Final mechanism hypothesis

Frozen hypothesis: channel-specific physical withdrawal of donor authority can expose
receiver takeover capacity only if the withdrawn channel's measured donor effect falls
while non-withdrawn effects and receiver commands remain stable.  Capacity, desired
responsibility, commanded allocation, and measured contribution are distinct objects.

This first Stage 2E execution is limited to code conformance and withdrawal validity.
It does not implement a capacity classifier, allocator, release state machine, or closed
loop.  A finite rollout, contact, or monotone command is not sufficient: the physical
effect and isolation gates in `EXPERIMENT_CONTRACT.yaml` must pass.

Historical Stage 2/2B/2C/2D negative decisions remain frozen.  `accepted=false`.
