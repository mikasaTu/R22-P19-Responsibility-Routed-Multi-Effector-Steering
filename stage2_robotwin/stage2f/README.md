# Stage 2F / Feishu Step8

This subtree implements the preregistered calibration-only Physical Authority Knob
Feasibility checkpoint. Repository report filenames retain the plan's frozen
`STEP7_*` spelling, while the Feishu hierarchy is `实验规划/step8/实验报告`.

Delivery 1 contains the frozen contract, K1/K2/K3 implementations, deterministic
gate utilities, unit tests, and a two-cell dev14 smoke entry point. It does not
authorize the 144+10 matrix; delivery 2 requires explicit user confirmation.

All outputs remain `accepted=false` and `pai_job_created=false`.

